<!-- if you want to render an image put it's link and then click ; ctrl+shift+v -->

<!-- Variables
it's  a name for data that has  address in the memory

rules to write varaibles;

you can't start with variable from  0-9
you can't start with 2 underscores
you can't start with underscore followed by capital letter.

Pre-and-Post Increment-and-Decrement
i++; // Add one to i (post-increment)
i--; // Subtract one from i (post-decrement)
also they equal to ;
i += 1; // Add one to i
i -= 1; // Subtract one from i
++i;      pre increment
--i; pre decrement.
---------------
Conditional operators:
a == b; // True if a is equivalent to b
a != b; // True if a is not equivalent to b
a < b; // True if a is less than b
a > b; // True if a is greater than b
a <= b; // True if a is less than or equal to b
a >= b; // True if a is greater than or equal to b
-----------
Operator Boolean meaning
&& and
|| or
! not
----------
What is Sizeof?
function render the size of  the variable in memory  or whatever you want in bytes..
    output: unsigned int
also it's called; size-t(size-tee) 
--------
flowcontrols like;
    for
    while
    do while
    if

 -->

